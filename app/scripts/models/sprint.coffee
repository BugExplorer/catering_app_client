define [
  'underscore'
  'backbone'
], (_, Backbone) ->
  'use strict';

  class SprintModel extends Backbone.Model

    # defaults: {}

    # validate: (attrs, options) ->

    # parse: (response, options) ->
    #   response
